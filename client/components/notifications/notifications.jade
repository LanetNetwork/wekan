template(name='notifications')
  #notifications.board-header-btns.right
    //- a.notifications-drawer-toggle.fa.fa-bell(class="{{#if $gt unreadNotifications 0}}alert{{/if}}" title="{{_ 'notifications'}}")
    .bell-block(class="{{#if $gt unreadNotifications 0}}alert{{/if}}")
      img(class="notifications-drawer-toggle norm"  src="/custom/bell-ring-outline.svg", alt="" title="{{_ 'notifications'}}")
      img(class="notifications-drawer-toggle red"  src="/custom/bell-ring-outline-red.svg", alt="" title="{{_ 'notifications'}}")
      if $.Session.get 'showNotificationsDrawer'
        +notificationsDrawer(unreadNotifications=unreadNotifications)
