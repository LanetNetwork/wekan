name: Build
on: push
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prepare to build
        run: sed -i 's/#build:/build:/; s/#  context:/  context:/; s/#  dockerfile:/  dockerfile:/' docker-compose.yml
      - name: Run docker compose build
        run: sudo docker compose up -d --build
